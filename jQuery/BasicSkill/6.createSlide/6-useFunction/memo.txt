成果物自体は５までと全く同じだが、「toggleChangeBtn」という関数に「.change-btn」要素の表示/非表示の処理をまとめていき、コードをスマートにしている。
５のscript.jsでは全く同じ処理をクリックイベントで複数箇所に設定してある。
これでスライド機能は一通り完成。
しかしまだ改善点があります。
今のままだとスライドの枚数が変わると、「次へ」ボタンを隠す機能が壊れてしまいます。これをスライドの枚数が変わっても壊れないようにしましょう。そこに関しては７へ。